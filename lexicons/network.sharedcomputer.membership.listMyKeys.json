{
  "lexicon": 1,
  "id": "network.sharedcomputer.membership.listMyKeys",
  "defs": {
    "main": {
      "type": "query",
      "description": "The caller's gateway API keys.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["keys"],
          "properties": {
            "keys": { "type": "array", "items": { "type": "unknown" } }
          }
        }
      }
    }
  }
}
