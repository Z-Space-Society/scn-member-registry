{
  "lexicon": 1,
  "id": "network.sharedcomputer.membership.request",
  "defs": {
    "main": {
      "type": "record",
      "description": "A membership application, written to the applicants own PDS.",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["createdAt"],
        "properties": {
          "note": {
            "type": "string",
            "description": "Optional short introduction. Public.",
            "maxLength": 1000,
            "maxGraphemes": 300
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
